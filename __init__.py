"""Package for the Australian news highlights application.

This package exposes the main Streamlit app as ``app.py`` and helper
modules ``news_fetcher`` and ``article_processor``.  It can be
installed as a library or run directly.
"""
